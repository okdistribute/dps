usage: dps <command(s)> [--flag] [--key=value]

commands:
  add       download source
  check     get metadata about a source
  update    update previously downloaded sources
  rm        remove a source
  status    get status on current sources
  destroy   remove everything!

type `dps <command> --help` to view specific command help
